SELECT student_attend_placements.CompanyName,student.RollNumber, bsp_code.Branch 
FROM student 
INNER JOIN bsp_code ON student.BSP=bsp_code.BSP 
INNER JOIN student_attend_placements ON student.RollNumber=student_attend_placements.RollNumber 
where student_attend_placements.Result='Placed'


SELECT student_attend_placements.CompanyName, bsp_code.Branch ,count(bsp_code.Branch) as Count
FROM student 
INNER JOIN student_attend_placements ON student.RollNumber=student_attend_placements.RollNumber 
INNER JOIN bsp_code ON student.BSP=bsp_code.BSP 
where student_attend_placements.Result='Placed'
GROUP BY student_attend_placements.CompanyName, bsp_code.Branch
ORDER BY student_attend_placements.CompanyName	



SELECT dailyattendance.RollNumber, courses.CourseName, dailyattendance.Timeperiod, dailyattendance.Attendance, COUNT(dailyattendance.Attendance) AS AttendanceCount
from dailyattendance
INNER JOIN courses
ON courses.CourseID=dailyattendance.CourseID
WHERE dailyattendance.RollNumber='160114733313' AND dailyattendance.Timeperiod=(SELECT max(id) from timeperiod)
GROUP by courses.CourseName, dailyattendance.Attendance


php artisan make:migration create_student_details_table --create=student_details

ToDO
------
timetable
staff_courses


SELECT timetable.Day,timetable.Timeslot,timetable.StaffID,staff.FullName,courses.CourseName, timetable.CourseID, timetable.Batch
FROM timetable 
INNER JOIN staff
ON timetable.StaffID = staff.StaffID 
WHERE  timetable.YearandSem = '4/4 Sem-2' AND timetable.BSP = (SELECT bsp_code.BSP FROM bsp_code WHERE bsp_code.Branch='CSE' AND bsp_code.Section='2' AND bsp_code.Program='BE') AND timetable.CourseID IN (SELECT student_enroll_courses.CourseID FROM student_enroll_courses WHERE student_enroll_courses.RollNumber='160114733313') AND timetable.Batch = 0 OR 2



SELECT DISTINCT staff_teaches_courses.YearandSem, bsp_code.Branch, bsp_code.Program, staff_teaches_courses.Batch FROM staff_teaches_courses INNER JOIN bsp_code ON bsp_code.BSP = staff_teaches_courses.BSP WHERE StaffID='3' AND CourseID='CS471' AND Timeperiod=(SELECT max(id) FROM timeperiod)